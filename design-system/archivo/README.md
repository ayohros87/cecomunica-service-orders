# Archivo — propuestas cerradas (nada aquí está pendiente)

> **Todo lo que hay en esta carpeta ya fue implementado en la app o descartado.**
> No representa trabajo futuro ni un look "por venir". El diseño vigente de la
> página es el que describe `../README.md` y el que está implementado en
> `public/css/` (ceco-ui, ceco-command, etc.).

Se conserva como referencia histórica de dónde salió cada pieza:

| Carpeta | Qué fue | Dónde vive hoy |
|---|---|---|
| `logo-concepts/` | Exploración de monogramas CC | El elegido es el monograma oficial: `public/brand/cecomunica-monogram*.svg` |
| `new-logo/app-icons/` | Entrega de íconos de app (copia idéntica) | Shippeado en `public/brand/app-icons/` y favicons de la raíz de `public/` |
| `rediseno-command-center/` | Propuesta del rediseño Command Center | Implementado 2026-07-14: `public/css/ceco-command.css`, rail, home con señales |
| `cotizacion/` | UI kit del módulo de cotizaciones (JSX de referencia) | Portado a `public/css/cotizaciones-kit.css` y `print-cotizacion.css` |
| `firma-y-cotizacion/` | Entrega de firma de correo + colateral | Shippeado como `public/firma-correo.html` |
| `emails/` | Plantillas de correo (entrega) | Portado a `functions/templates/email-base.html` |

Si alguna vez se retoma un rediseño, empieza una carpeta nueva en la raíz del
design system — no edites estas.
